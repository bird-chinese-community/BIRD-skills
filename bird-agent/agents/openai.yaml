interface:
  display_name: "BIRD Config Agent"
  short_description: "Write, validate, format, and debug BIRD routing daemon configs with parser-backed diagnostics."
  icon_small: "./assets/logo.svg"
  icon_large: "./assets/logo.svg"
  brand_color: "#3B82F6"

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
