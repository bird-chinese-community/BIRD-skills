interface:
  display_name: "BIRD Agent"
  short_description: "Assist with BIRD routing daemon configs using the BIRDCC toolchain."
  brand_color: "#3B82F6"

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
