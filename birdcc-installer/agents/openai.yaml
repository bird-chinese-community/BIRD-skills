interface:
  display_name: 'BIRD Tooling Installer'
  short_description: 'Install BIRD editor support and the birdcc CLI.'
  icon_small: './assets/logo-small.png'
  icon_large: './assets/logo.png'
  brand_color: '#8B5CF6'

instructions: |
  When using scripts/detect_ide.py, only pass --install --confirmed after the user has explicitly
  approved the installation in chat. Never pre-confirm on the user's behalf.

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
